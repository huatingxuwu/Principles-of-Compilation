============================================================
  测试: 算术表达式文法 (经典 LR 示例)
============================================================

────────────────────────────────────────────────────────────
  第一步: 解析原文法
────────────────────────────────────────────────────────────
  开始符号: E
  非终结符: ['E', 'F', 'T']
  终结符:   ['(', ')', '*', '+', 'id']
  产生式:
  E → E + T
  E → T
  T → T * F
  T → F
  F → ( E )
  F → id

────────────────────────────────────────────────────────────
  第二步: 构造 LR(0) 自动机
────────────────────────────────────────────────────────────
增广文法: 添加 E' → E

初始项: E' → · E

计算 CLOSURE({E' → · E}):
      由 E' → · E 预测: 加入 E → · E + T
      由 E' → · E 预测: 加入 E → · T
      由 E → · T 预测: 加入 T → · T * F
      由 E → · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
  → I₀ = {E → · E + T,  E → · T,  E' → · E,  F → · ( E ),  F → · id,  T → · F,  T → · T * F}

── 处理状态 I0 ──
  项集: {E → · E + T,  E → · T,  E' → · E,  F → · ( E ),  F → · id,  T → · F,  T → · T * F}

  GOTO(I0, '('):
      kernel 项 (点右移过 '('):
        F → ( · E )
      由 F → ( · E ) 预测: 加入 E → · E + T
      由 F → ( · E ) 预测: 加入 E → · T
      由 E → · T 预测: 加入 T → · T * F
      由 E → · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 新状态 I1 = {E → · E + T,  E → · T,  F → · ( E ),  F → ( · E ),  F → · id,  T → · F,  T → · T * F}

  GOTO(I0, 'E'):
      kernel 项 (点右移过 'E'):
        E → E · + T
        E' → E ·
    → 新状态 I2 = {E → E · + T,  E' → E ·}

  GOTO(I0, 'F'):
      kernel 项 (点右移过 'F'):
        T → F ·
    → 新状态 I3 = {T → F ·}

  GOTO(I0, 'T'):
      kernel 项 (点右移过 'T'):
        E → T ·
        T → T · * F
    → 新状态 I4 = {E → T ·,  T → T · * F}

  GOTO(I0, 'id'):
      kernel 项 (点右移过 'id'):
        F → id ·
    → 新状态 I5 = {F → id ·}

── 处理状态 I1 ──
  项集: {E → · E + T,  E → · T,  F → · ( E ),  F → ( · E ),  F → · id,  T → · F,  T → · T * F}

  GOTO(I1, '('):
      kernel 项 (点右移过 '('):
        F → ( · E )
      由 F → ( · E ) 预测: 加入 E → · E + T
      由 F → ( · E ) 预测: 加入 E → · T
      由 E → · T 预测: 加入 T → · T * F
      由 E → · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 闭包后项集 = I1（已存在）

  GOTO(I1, 'E'):
      kernel 项 (点右移过 'E'):
        E → E · + T
        F → ( E · )
    → 新状态 I6 = {E → E · + T,  F → ( E · )}

  GOTO(I1, 'F'):
      kernel 项 (点右移过 'F'):
        T → F ·
    → 闭包后项集 = I3（已存在）

  GOTO(I1, 'T'):
      kernel 项 (点右移过 'T'):
        E → T ·
        T → T · * F
    → 闭包后项集 = I4（已存在）

  GOTO(I1, 'id'):
      kernel 项 (点右移过 'id'):
        F → id ·
    → 闭包后项集 = I5（已存在）

── 处理状态 I2 ──
  项集: {E → E · + T,  E' → E ·}

  GOTO(I2, '+'):
      kernel 项 (点右移过 '+'):
        E → E + · T
      由 E → E + · T 预测: 加入 T → · T * F
      由 E → E + · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 新状态 I7 = {E → E + · T,  F → · ( E ),  F → · id,  T → · F,  T → · T * F}

── 处理状态 I3 ──
  项集: {T → F ·}

── 处理状态 I4 ──
  项集: {E → T ·,  T → T · * F}

  GOTO(I4, '*'):
      kernel 项 (点右移过 '*'):
        T → T * · F
      由 T → T * · F 预测: 加入 F → · ( E )
      由 T → T * · F 预测: 加入 F → · id
    → 新状态 I8 = {F → · ( E ),  F → · id,  T → T * · F}

── 处理状态 I5 ──
  项集: {F → id ·}

── 处理状态 I6 ──
  项集: {E → E · + T,  F → ( E · )}

  GOTO(I6, ')'):
      kernel 项 (点右移过 ')'):
        F → ( E ) ·
    → 新状态 I9 = {F → ( E ) ·}

  GOTO(I6, '+'):
      kernel 项 (点右移过 '+'):
        E → E + · T
      由 E → E + · T 预测: 加入 T → · T * F
      由 E → E + · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 闭包后项集 = I7（已存在）

── 处理状态 I7 ──
  项集: {E → E + · T,  F → · ( E ),  F → · id,  T → · F,  T → · T * F}

  GOTO(I7, '('):
      kernel 项 (点右移过 '('):
        F → ( · E )
      由 F → ( · E ) 预测: 加入 E → · E + T
      由 F → ( · E ) 预测: 加入 E → · T
      由 E → · T 预测: 加入 T → · T * F
      由 E → · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 闭包后项集 = I1（已存在）

  GOTO(I7, 'F'):
      kernel 项 (点右移过 'F'):
        T → F ·
    → 闭包后项集 = I3（已存在）

  GOTO(I7, 'T'):
      kernel 项 (点右移过 'T'):
        E → E + T ·
        T → T · * F
    → 新状态 I10 = {E → E + T ·,  T → T · * F}

  GOTO(I7, 'id'):
      kernel 项 (点右移过 'id'):
        F → id ·
    → 闭包后项集 = I5（已存在）

── 处理状态 I8 ──
  项集: {F → · ( E ),  F → · id,  T → T * · F}

  GOTO(I8, '('):
      kernel 项 (点右移过 '('):
        F → ( · E )
      由 F → ( · E ) 预测: 加入 E → · E + T
      由 F → ( · E ) 预测: 加入 E → · T
      由 E → · T 预测: 加入 T → · T * F
      由 E → · T 预测: 加入 T → · F
      由 T → · F 预测: 加入 F → · ( E )
      由 T → · F 预测: 加入 F → · id
    → 闭包后项集 = I1（已存在）

  GOTO(I8, 'F'):
      kernel 项 (点右移过 'F'):
        T → T * F ·
    → 新状态 I11 = {T → T * F ·}

  GOTO(I8, 'id'):
      kernel 项 (点右移过 'id'):
        F → id ·
    → 闭包后项集 = I5（已存在）

── 处理状态 I9 ──
  项集: {F → ( E ) ·}

── 处理状态 I10 ──
  项集: {E → E + T ·,  T → T · * F}

  GOTO(I10, '*'):
      kernel 项 (点右移过 '*'):
        T → T * · F
      由 T → T * · F 预测: 加入 F → · ( E )
      由 T → T * · F 预测: 加入 F → · id
    → 闭包后项集 = I8（已存在）

── 处理状态 I11 ──
  项集: {T → T * F ·}

=======================================================
LR(0) 自动机汇总
=======================================================

  状态 I0:
    E → · E + T
    E → · T
    E' → · E
    F → · ( E )
    F → · id
    T → · F
    T → · T * F
    出边:
      --(--> I1
      --E--> I2
      --F--> I3
      --T--> I4
      --id--> I5

  状态 I1:
    E → · E + T
    E → · T
    F → · ( E )
    F → ( · E )
    F → · id
    T → · F
    T → · T * F
    出边:
      --(--> I1
      --E--> I6
      --F--> I3
      --T--> I4
      --id--> I5

  状态 I2:
    E → E · + T
    E' → E ·  ← 接受项 (ACC)
    出边:
      --+--> I7

  状态 I3:
    T → F ·  ← 归约项 (可归约)

  状态 I4:
    E → T ·  ← 归约项 (可归约)
    T → T · * F
    出边:
      --*--> I8

  状态 I5:
    F → id ·  ← 归约项 (可归约)

  状态 I6:
    E → E · + T
    F → ( E · )
    出边:
      --)--> I9
      --+--> I7

  状态 I7:
    E → E + · T
    F → · ( E )
    F → · id
    T → · F
    T → · T * F
    出边:
      --(--> I1
      --F--> I3
      --T--> I10
      --id--> I5

  状态 I8:
    F → · ( E )
    F → · id
    T → T * · F
    出边:
      --(--> I1
      --F--> I11
      --id--> I5

  状态 I9:
    F → ( E ) ·  ← 归约项 (可归约)

  状态 I10:
    E → E + T ·  ← 归约项 (可归约)
    T → T · * F
    出边:
      --*--> I8

  状态 I11:
    T → T * F ·  ← 归约项 (可归约)



============================================================
  测试: 含 ε 的简单文法
============================================================

────────────────────────────────────────────────────────────
  第一步: 解析原文法
────────────────────────────────────────────────────────────
  开始符号: S
  非终结符: ['A', 'B', 'S']
  终结符:   ['a', 'b']
  产生式:
  S → A B
  A → a
  A → ε
  B → b
  B → ε

────────────────────────────────────────────────────────────
  第二步: 构造 LR(0) 自动机
────────────────────────────────────────────────────────────
增广文法: 添加 S' → S

初始项: S' → · S

计算 CLOSURE({S' → · S}):
      由 S' → · S 预测: 加入 S → · A B
      由 S → · A B 预测: 加入 A → · a
      由 S → · A B 预测: 加入 A → · ε
  → I₀ = {A → · a,  A → · ε,  S → · A B,  S' → · S}

── 处理状态 I0 ──
  项集: {A → · a,  A → · ε,  S → · A B,  S' → · S}

  GOTO(I0, 'A'):
      kernel 项 (点右移过 'A'):
        S → A · B
      由 S → A · B 预测: 加入 B → · b
      由 S → A · B 预测: 加入 B → · ε
    → 新状态 I1 = {B → · b,  B → · ε,  S → A · B}

  GOTO(I0, 'S'):
      kernel 项 (点右移过 'S'):
        S' → S ·
    → 新状态 I2 = {S' → S ·}

  GOTO(I0, 'a'):
      kernel 项 (点右移过 'a'):
        A → a ·
    → 新状态 I3 = {A → a ·}

── 处理状态 I1 ──
  项集: {B → · b,  B → · ε,  S → A · B}

  GOTO(I1, 'B'):
      kernel 项 (点右移过 'B'):
        S → A B ·
    → 新状态 I4 = {S → A B ·}

  GOTO(I1, 'b'):
      kernel 项 (点右移过 'b'):
        B → b ·
    → 新状态 I5 = {B → b ·}

── 处理状态 I2 ──
  项集: {S' → S ·}

── 处理状态 I3 ──
  项集: {A → a ·}

── 处理状态 I4 ──
  项集: {S → A B ·}

── 处理状态 I5 ──
  项集: {B → b ·}

=======================================================
LR(0) 自动机汇总
=======================================================

  状态 I0:
    A → · a
    A → · ε
    S → · A B
    S' → · S
    出边:
      --A--> I1
      --S--> I2
      --a--> I3

  状态 I1:
    B → · b
    B → · ε
    S → A · B
    出边:
      --B--> I4
      --b--> I5

  状态 I2:
    S' → S ·  ← 接受项 (ACC)

  状态 I3:
    A → a ·  ← 归约项 (可归约)

  状态 I4:
    S → A B ·  ← 归约项 (可归约)

  状态 I5:
    B → b ·  ← 归约项 (可归约)



